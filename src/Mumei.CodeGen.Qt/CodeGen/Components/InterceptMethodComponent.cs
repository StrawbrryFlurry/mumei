namespace Mumei.CodeGen.Qt;

internal static partial class CodeGen {
    // Since all intercept methods share the same general structure, we should
    // be able to transform the template definition to a common structure that can be
    // constructed by a shared component implementation.
    internal sealed class GenericInterceptMethodComponent { }
}